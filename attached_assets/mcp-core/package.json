{
  "name": "mcp-core",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    // Dependencies will be added as identified during refactoring,
    // likely including @modelcontextprotocol/sdk, chokidar, deep-equal, fs/promises, path, vscode, zod, etc.
    // Also need @shared/* and @utils/* from the main package (managed via tsconfig paths or workspace setup).
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
